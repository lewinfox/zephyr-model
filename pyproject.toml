[project]
name = "zephyr-model"
version = "0.1.0"
description = "Paragliding conditions forecasting"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ruff>=0.14.6",
    "tsai>=0.4.1",
]
